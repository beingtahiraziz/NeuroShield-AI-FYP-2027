
════════════════════════════════════════════════════════════════
  1. SDK & dependency versions
════════════════════════════════════════════════════════════════

Python MCP/Anthropic pins found:
    mcp-servers/requirements.txt:7:mcp>=0.1.0
    mcp-servers/requirements.txt:8:fastmcp>=0.1.0

TypeScript MCP SDK pins found:
    (none in tree — npx-launched servers resolve at runtime)

  ↳ Action: track Python + TS SDK releases during the RC window;
    Tier 1 SDKs ship 2026-07-28 support before the July 28 final.

════════════════════════════════════════════════════════════════
  2. Hard breaking changes
════════════════════════════════════════════════════════════════
[ok ] Custom error code -32002 (resource not found)

[FIX ] Experimental Tasks API (esp. tasks/list — removed) — 4 hit(s)
  ↳ Tasks moved to an extension with a new lifecycle (tasks/get|update|cancel, server-directed creation). tasks/list is gone.
      tools/cape_sandbox.py:235:                        f"{base}/apiv2/tasks/create/file/",
      tools/cape_sandbox.py:250:                    f"{base}/apiv2/tasks/create/file/",
      tools/cape_sandbox.py:272:                f"{base}/apiv2/tasks/create/url/",
      tools/cape_sandbox.py:323:                f"{base}/apiv2/tasks/list/{limit}/{offset}/",
[ok ] Hardcoded protocolVersion pins

[WARN] Session-ID / handshake assumptions in custom transport code — 1 hit(s)
  ↳ SEP-2567/2575: session header and initialize handshake removed. Only matters if you wrote custom HTTP transport/client code (the SDK handles stdio).
      core/telemetry.py:267:    initialized. Returns True if OTEL was actually enabled and initialized.

════════════════════════════════════════════════════════════════
  3. Deprecated core features (12-month removal window)
════════════════════════════════════════════════════════════════
[ok ] MCP-level logging
[ok ] Sampling (server-initiated LLM calls)
[ok ] Roots

════════════════════════════════════════════════════════════════
  4. Elicitation / approval-flow touchpoints
════════════════════════════════════════════════════════════════
[ok ] Elicitation usage (approval workflow!)

[INFO] Long-running tool calls (Tasks-extension candidates) — 57 hit(s)
  ↳ Sandbox submissions (Joe/ANY.RUN/Hybrid) are natural Tasks candidates. Target the new extension, not the 2025-11-25 experimental API.
      daemon/llm_worker_manager.py:53:                await asyncio.wait_for(
      tools/cape_sandbox.py:5:CAPEv2 REST API so SOC agents can submit files/URLs for detonation and
      tools/cape_sandbox.py:100:            name="cape_submit_file",
      tools/cape_sandbox.py:102:                "Submit a file to CAPE Sandbox for detonation. "
      tools/cape_sandbox.py:132:            name="cape_submit_url",
      tools/cape_sandbox.py:133:            description="Submit a URL to CAPE Sandbox for behavioral analysis.",
      tools/cape_sandbox.py:221:        if name == "cape_submit_file":
      tools/cape_sandbox.py:244:                        {"error": "filename required when submitting via file_b64"}
      tools/cape_sandbox.py:264:        if name == "cape_submit_url":
      daemon/kafka_ingestor.py:114:                await asyncio.wait_for(
      daemon/kafka_ingestor.py:130:                await asyncio.wait_for(self._task, timeout=30)
      daemon/scheduler.py:17:    return os.getenv("SANDBOX_AUTO_SUBMIT", "false").strip().lower() in ("1", "true", "yes", "on")
      daemon/scheduler.py:101:        # Sandbox poller — only runs when auto-submit is enabled
      daemon/scheduler.py:181:                await asyncio.wait_for(shutdown_event.wait(), timeout=60)
      daemon/agent_runner.py:450:                        await asyncio.sleep(30)
      daemon/federation/runner.py:130:                    await asyncio.wait_for(shutdown_event.wait(), timeout=idle_seconds)
      daemon/federation/runner.py:148:                await asyncio.wait_for(shutdown_event.wait(), timeout=sleep_for)
      daemon/sandbox_submitter.py:6:1. Respects the master ``SANDBOX_AUTO_SUBMIT`` switch (default: disabled).
      daemon/sandbox_submitter.py:8:3. Checks each enabled sandbox's hash cache first — only submits when unknown.
      daemon/sandbox_submitter.py:68:            auto_submit=_env_bool("SANDBOX_AUTO_SUBMIT", False),
      daemon/sandbox_submitter.py:79:class SandboxSubmitter:
      daemon/sandbox_submitter.py:153:        if not self.is_hash_safe_to_submit(hash_val, file_hint):
      daemon/poller.py:254:                await asyncio.wait_for(
      daemon/poller.py:380:                await asyncio.wait_for(
      daemon/poller.py:566:                await asyncio.wait_for(shutdown_event.wait(), timeout=interval)
      ... (32 more)

════════════════════════════════════════════════════════════════
  5. Transport & caching posture
════════════════════════════════════════════════════════════════

[INFO] HTTP/SSE transports in use — 1 hit(s)
  ↳ Any server exposed over HTTP needs Mcp-Method/Mcp-Name headers (SEP-2243) and the stateless model. stdio servers are unaffected.
      services/mcp_client.py:173:            mcp_service: MCPService instance for managing server processes

[INFO] tools/list caching in the client — 86 hit(s)
  ↳ New ttlMs/cacheScope fields (SEP-2549) give a spec-compliant cache policy — worth adopting with 100+ tools across servers.
      tools/vstrike.py:67:@server.list_tools()
      tools/vstrike.py:68:async def handle_list_tools():
      scripts/vstrike_list_tools.py:3:Calls `tools/list` against the live VStrike MCP endpoint and diffs the
      scripts/vstrike_list_tools.py:13:    python scripts/vstrike_list_tools.py
      scripts/vstrike_list_tools.py:79:        tools: List[Dict[str, Any]] = svc.list_tools()
      scripts/vstrike_list_tools.py:81:        print(f"tools/list failed: {e}", file=sys.stderr)
      tools/url_analysis.py:19:@server.list_tools()
      tools/url_analysis.py:20:async def handle_list_tools():
      tools/slack.py:23:@server.list_tools()
      tools/slack.py:24:async def handle_list_tools():
      tools/palo_alto.py:19:@server.list_tools()
      tools/palo_alto.py:20:async def handle_list_tools():
      tools/carbon_black.py:19:@server.list_tools()
      tools/carbon_black.py:20:async def handle_list_tools():
      tools/misp.py:23:@server.list_tools()
      tools/misp.py:24:async def handle_list_tools():
      tools/microsoft_teams.py:19:@server.list_tools()
      tools/microsoft_teams.py:20:async def handle_list_tools():
      tools/microsoft_defender.py:38:@server.list_tools()
      tools/microsoft_defender.py:39:async def handle_list_tools():
      tools/cape_sandbox.py:96:@server.list_tools()
      tools/cape_sandbox.py:97:async def handle_list_tools() -> List[types.Tool]:
      tools/ip_geolocation.py:19:@server.list_tools()
      tools/ip_geolocation.py:20:async def handle_list_tools():
      tools/hybrid_analysis.py:19:@server.list_tools()
      ... (61 more)

════════════════════════════════════════════════════════════════
  6. mcp-config.json hygiene
════════════════════════════════════════════════════════════════

Unpinned npx/uvx launchers (will silently pick up new-spec releases):
    56-      "args": ["-y", "security-detections-mcp"],
    89-      "args": ["-y", "mcp-remote", "https://sentinelmcp.microsoft.com/v1/mcp"],
    97-      "args": ["-y", "mcp-remote", "${SPLUNK_MCP_URL}"],
    143-      "args": ["-y", "mcp-remote", "https://mcp.atlassian.com/v1/mcp"],
  ↳ Consider pinning (pkg@x.y.z) until you've validated each against the new SDK.

════════════════════════════════════════════════════════════════
  Summary
════════════════════════════════════════════════════════════════

  Categories with findings: 5
  Severities: FIX = breaks on 2026-07-28 · WARN = deprecated/fragile
              PLAN = design work needed · INFO = opportunity

  Spec/changelog: https://modelcontextprotocol.io/specification/draft/changelog
