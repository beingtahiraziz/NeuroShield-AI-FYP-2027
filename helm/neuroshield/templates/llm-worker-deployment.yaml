{{- if .Values.llmWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "neuroshield.llmWorker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "neuroshield.componentLabels" (dict "context" . "component" "llm-worker") | nindent 4 }}
spec:
  {{- if not .Values.llmWorker.autoscaling.enabled }}
  replicas: {{ .Values.llmWorker.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "neuroshield.componentSelectorLabels" (dict "context" . "component" "llm-worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "neuroshield.componentLabels" (dict "context" . "component" "llm-worker") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.llmWorker.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "neuroshield.serviceAccountName" . }}
      {{- include "neuroshield.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: llm-worker
          image: {{ include "neuroshield.image" (dict "context" . "component" "llmWorker") | quote }}
          imagePullPolicy: {{ include "neuroshield.imagePullPolicy" (dict "context" . "component" "llmWorker") }}
          command:
            - python
            - -m
            - services.run_llm_worker
          envFrom:
            {{- include "neuroshield.envFrom" . | nindent 12 }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "neuroshield.postgres.passwordSecret" . }}
                  key: {{ include "neuroshield.postgres.passwordSecretKey" . }}
            {{- include "neuroshield.env" . | nindent 12 }}
            {{- with .Values.llmWorker.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.llmWorker.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
      {{- with .Values.llmWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.llmWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.llmWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
