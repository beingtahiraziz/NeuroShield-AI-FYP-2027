{{- if and .Values.postgresql.enabled (not .Values.postgresql.bitnami.enabled) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "neuroshield.postgres.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "neuroshield.componentLabels" (dict "context" . "component" "postgres") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: {{ .Values.postgresql.service.port }}
      targetPort: postgres
  selector:
    {{- include "neuroshield.componentSelectorLabels" (dict "context" . "component" "postgres") | nindent 4 }}
{{- end }}
